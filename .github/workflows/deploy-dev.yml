name: Deploy DEV to Vercel

on:
  push:
    branches:
      - main
  workflow_dispatch:

concurrency:
  group: vercel-dev-${{ github.ref }}
  cancel-in-progress: true

env:
  VERCEL_ORG_ID: ${{ secrets.VERCEL_ORG_ID }}
  VERCEL_PROJECT_ID: ${{ secrets.VERCEL_PROJECT_ID }}
  VERCEL_DEV_ALIAS: ${{ vars.VERCEL_DEV_ALIAS }}

jobs:
  deploy-dev:
    name: Deploy DEV
    runs-on: ubuntu-latest
    environment:
      name: DEV
    permissions:
      contents: read
      deployments: write
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: yarn

      - name: Enable Corepack
        run: corepack enable

      - name: Install dependencies
        run: yarn install --frozen-lockfile

      - name: Lint
        run: yarn lint

      - name: Typecheck
        run: yarn typecheck

      - name: Install Vercel CLI
        run: npm install --global vercel@latest

      - name: Pull Vercel DEV environment
        run: vercel pull --yes --environment=preview --token=${{ secrets.VERCEL_TOKEN }}

      - name: Build Vercel artifacts
        run: vercel build --token=${{ secrets.VERCEL_TOKEN }}

      - name: Deploy DEV artifacts
        id: deploy
        run: |
          deployment_url="$(vercel deploy --prebuilt --target=preview --token=${{ secrets.VERCEL_TOKEN }})"
          echo "url=$deployment_url" >> "$GITHUB_OUTPUT"
          echo "DEV deployment: $deployment_url"

      - name: Assign stable DEV alias
        id: alias
        if: env.VERCEL_DEV_ALIAS != ''
        run: |
          vercel alias set "${{ steps.deploy.outputs.url }}" "$VERCEL_DEV_ALIAS" --token=${{ secrets.VERCEL_TOKEN }}
          echo "url=https://$VERCEL_DEV_ALIAS" >> "$GITHUB_OUTPUT"
          echo "Stable DEV URL: https://$VERCEL_DEV_ALIAS"

      - name: Add deployment summary
        run: |
          echo "### Vercel DEV deployment" >> "$GITHUB_STEP_SUMMARY"
          echo "" >> "$GITHUB_STEP_SUMMARY"
          echo "Deployment URL: ${{ steps.deploy.outputs.url }}" >> "$GITHUB_STEP_SUMMARY"
          if [ -n "${{ steps.alias.outputs.url }}" ]; then
            echo "" >> "$GITHUB_STEP_SUMMARY"
            echo "Stable DEV URL: ${{ steps.alias.outputs.url }}" >> "$GITHUB_STEP_SUMMARY"
          fi
